"""Database tables for wallet pass tracking.

DDL has moved to db.py. This module is kept for import compatibility.
"""


def init_wallet_tables(db):
    """No-op — all DDL is now managed by db.init_db()."""
    pass
